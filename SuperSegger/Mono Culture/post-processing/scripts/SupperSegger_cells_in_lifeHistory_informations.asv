
%find .mat files in cell directory
directory="../../xy1/cell/";
files=dir([directory+"*.mat"]);
%sorted files
sorted_files = natsortfiles(files);
%number of files
Num_files=length(sorted_files);

for i=1:Num_files
    file_name=sorted_files(i).name;
    % load `.mat` file
    load(directory+file_name,'CellA');
    life_history=length(CellA);
    
    for j=1:life_history
    orientation=CellA{1,j}.coord.orientation;
    length=CellA{1,j}.cellLength(1)
    pos=
    end
end